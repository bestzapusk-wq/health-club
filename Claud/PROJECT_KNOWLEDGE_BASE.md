# Health Club — Полная документация проекта

> **Для Claude:** Это база знаний о приложении. Используй её чтобы понимать структуру, логику и состояние проекта.

---

## 🎯 Что это за приложение

**Health Club** — мобильное PWA приложение для женщин 35-55 лет (Казахстан), которое:
1. Собирает данные о здоровье (опросник + анализы)
2. Генерирует AI-разбор через Claude API
3. Помогает вести здоровый образ жизни (дневник, трекеры, рецепты)
4. Продаёт платные программы и курсы

**Стек:** React 19 + Vite + Supabase + Claude API

---

## 📁 Структура проекта

```
src/
├── pages/           # 18 страниц приложения
├── components/      # UI компоненты по категориям
│   ├── fasting/     # Виджет голодания
│   ├── food/        # Модалки еды
│   ├── habits/      # Трекеры привычек
│   ├── layout/      # Header, BottomNav
│   ├── main/        # Компоненты главной
│   ├── materials/   # Секция курсов
│   ├── onboarding/  # Онбординг
│   ├── profile/     # Компоненты профиля
│   ├── register/    # Компоненты регистрации
│   ├── survey/      # Компоненты опросника
│   └── ui/          # Базовые UI (Button, Input, Modal)
├── data/            # Статичные данные (вопросы, рецепты, курсы)
├── hooks/           # Кастомные хуки
├── lib/             # Сервисы (Supabase, AI, рецепты)
├── utils/           # Утилиты (форматирование, валидация)
└── styles/          # Глобальные стили
```

---

## 🗄️ База данных (Supabase)

### Существующие таблицы:
| Таблица | Описание | Статус |
|---------|----------|--------|
| `profiles` | Профили пользователей (id, first_name, gender, birth_date, weight_kg, height_cm, phone) | ✅ Работает |
| `survey_responses` | Ответы на опросник | ✅ Работает |
| `uploaded_files` | Загруженные файлы анализов | ✅ Работает |
| `analysis_results` | Результаты AI-анализа | ✅ Работает |
| `daily_reports` | Ежедневные отчёты (вода, активность, сон) | ✅ Работает |
| `recipes` | Рецепты для плана питания | ✅ Работает |
| `fasting_settings` | Настройки интервального голодания | ⚠️ Нужно создать |
| `fasting_sessions` | История сессий голодания | ⚠️ Нужно создать |

### SQL для создания недостающих таблиц:
```sql
-- Настройки голодания
CREATE TABLE IF NOT EXISTS fasting_settings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES profiles(id) UNIQUE,
  mode TEXT NOT NULL DEFAULT '16:8',
  eating_window_start TIME NOT NULL DEFAULT '12:00',
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- История сессий голодания
CREATE TABLE IF NOT EXISTS fasting_sessions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES profiles(id),
  mode TEXT NOT NULL,
  started_at TIMESTAMP NOT NULL,
  ended_at TIMESTAMP NOT NULL,
  planned_hours INTEGER NOT NULL,
  actual_hours DECIMAL(4,2) NOT NULL,
  completed_fully BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);

-- RLS для обеих
ALTER TABLE fasting_settings ENABLE ROW LEVEL SECURITY;
ALTER TABLE fasting_sessions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users manage own" ON fasting_settings FOR ALL USING (auth.uid() = user_id);
CREATE POLICY "Users manage own" ON fasting_sessions FOR ALL USING (auth.uid() = user_id);
```

---

## 🔀 Роутинг (App.jsx)

| Путь | Страница | Описание |
|------|----------|----------|
| `/register` | RegisterPage | Регистрация/вход |
| `/` | MainPage | Главная (прогресс → трекеры) |
| `/survey` | SurveyPage | Опросник здоровья |
| `/report` | MyReportPage | AI-разбор анализов |
| `/food` | FoodPage | Дневник питания |
| `/food/plan` | MealPlanPage | План питания (свайп-рецепты) |
| `/food/recipes` | RecipesPage | Сохранённые рецепты |
| `/food/shopping` | ShoppingPage | Список продуктов |
| `/food/diary` | DiaryPage | Дневник (дублирует FoodPage?) |
| `/food/saved` | SavedPage | Сохранённые (?) |
| `/materials` | MaterialsPage | Программы, курсы, видео |
| `/program/:id` | ProgramDetailPage | Детали программы |
| `/course/:id` | CoursePage | Детали курса |
| `/profile` | ProfilePage | Профиль пользователя |
| `/profile/stats` | StatsPage | Статистика пользователя |
| `/profile/fasting` | FastingSettingsPage | Настройки голодания |
| `/vitamins` | VitaminsPage | Напоминания о витаминах |
| `/privacy` | PrivacyPage | Политика конфиденциальности |

---

## 📱 Главная страница (MainPage.jsx)

### Логика состояний:

```
progress.submitted === false:
  → Показываем прогресс-бар (Опросник → Анализы → Результат)
  → Карточки задач
  → Кнопка "Получить результаты"

progress.submitted === true:
  → Streak карточка (7 дней → подарок)
  → FastingWidget (таймер голодания)
  → HabitsTracker (вода, активность, сон)
  → DailyInsight (инсайт после заполнения)
```

### Откуда данные:
- `localStorage`: user_data, survey_completed, upload_completed, data_submitted
- `Supabase`: uploaded_files (count), daily_reports (streak)

---

## 📝 Опросник (SurveyPage + useSurvey hook)

### Как работает:
1. Вопросы из `data/surveyQuestions.js`
2. Ответы сохраняются в `useSurvey` hook
3. При завершении → `survey_responses` в Supabase
4. `localStorage.survey_completed = true`

### Типы вопросов:
- `yesno` — Да/Нет с картинкой
- `single` — Один вариант
- `multi` — Несколько вариантов
- `textarea` — Свободный текст

---

## 🤖 AI-анализ (MyReportPage + lib/generateReport.js)

### Как работает:
1. Пользователь нажимает "Получить результаты" на MainPage
2. Вызывается Edge Function `generate-analysis` в Supabase
3. Claude API анализирует опросник + файлы анализов
4. Результат сохраняется в `analysis_results`
5. MyReportPage загружает и отображает результат

### Структура результата:
```js
{
  main_findings: { summary, risk_level, health_score },
  critical_markers: [{ name, value, reference, interpretation }],
  warning_markers: [...],
  normal_markers: [...],
  body_systems: [{ system, status, findings, recommendations }],
  priorities: [{ title, description }],
  connection_chain: { connections, root_causes },
  good_news: [...]
}
```

---

## 🍽️ Дневник питания (FoodPage)

### Как работает:
- Данные в `localStorage.food_tracker`
- 4 приёма пищи: завтрак, обед, ужин, перекус
- При добавлении → AI анализирует фото еды через `foodAnalysisService.js`
- Модалки: AddFoodModal, FoodAnalysisModal

### Проблемы:
- ⚠️ Нет редактирования записей (только удаление)
- ⚠️ Есть DiaryPage — дублирует функционал?

---

## 🍳 План питания (MealPlanPage)

### Как работает:
- Рецепты из `Supabase.recipes` или fallback из `data/recipes.js`
- Свайп влево/вправо (как Tinder)
- Выбранные сохраняются в `localStorage.meal_plan`

### Проблемы:
- ⚠️ Кнопка Settings ничего не делает
- ⚠️ Свайпы не очевидны для пользователей

---

## ⏱️ Интервальное голодание (FastingWidget + FastingSettingsPage)

### Как работает:
1. Пользователь настраивает режим в `/profile/fasting` (16:8, 12:12, и т.д.)
2. Сохраняется в `fasting_settings` + `localStorage`
3. На MainPage показывается FastingWidget с таймером
4. Круговой прогресс показывает % прохождения фазы
5. "Завершить досрочно" → сохраняет сессию в `fasting_sessions`

### Логика фаз:
```
mode = "16:8", eating_start = "12:00"
→ Окно питания: 12:00 – 20:00 (8 часов)
→ Голодание: 20:00 – 12:00 (16 часов)

Сейчас 14:00? → phase = "eating", показываем сколько до 20:00
Сейчас 22:00? → phase = "fasting", показываем сколько до 12:00
```

---

## 📊 Трекер привычек (HabitsTracker)

### Как работает:
1. Три метрики: вода (мл), активность (мин), сон (часы)
2. При изменении → сохраняется в `daily_reports` (Supabase)
3. При нажатии "Отправить" → `submitted_at` заполняется
4. После отправки → показывается DailyInsight

### DailyInsight логика:
- Сон < 7 часов 3+ дня → предупреждение
- Вода < 1500мл 3+ дня → предупреждение
- Активность = 0 минут 3+ дня → предупреждение
- Всё ок → "Отличный день!"

---

## 👤 Профиль (ProfilePage)

### Структура:
1. **Шапка** — имя, вес, ИМТ статус, кнопка редактирования
2. **Моя статистика** → `/profile/stats`
3. **Интервальное голодание** → `/profile/fasting`
4. **Витамины** → `/vitamins`
5. **Уведомления** — toggle
6. **Меню** — Личные данные, Анализы, Поддержка, и т.д.
7. **Выход**

### ИМТ расчёт:
```js
BMI = weight / (height/100)²
< 18.5 → "Недостаточный вес"
18.5-24.9 → "Здоровый вес ✓"
25-29.9 → "Избыточный вес"
≥ 30 → "Ожирение"
```

---

## 💊 Витамины (VitaminsPage)

### Как работает:
- Данные в `localStorage.health_tracker_data.vitamins`
- CRUD операции (добавить, редактировать, удалить)
- Toggle напоминания

### Проблемы:
- ⚠️ Удаление без подтверждения
- ⚠️ Push-уведомления не реализованы (только UI)

---

## 🎓 Материалы (MaterialsPage)

### Секции:
1. **Программы** — платные, слайдер, ведут на ProgramDetailPage
2. **Семинары** — бесплатные, регистрация в localStorage
3. **Эфиры** — видео (не кликабельны!)
4. **Видео** — YouTube ссылки

### Проблемы:
- ⚠️ Эфиры (STREAMS) не открываются
- ⚠️ Регистрация на семинары только в localStorage

---

## 📚 Курсы (CoursePage)

### Как работает:
- Данные из `data/courses.js`
- Показывает: трейлер, описание, модули, уроки, цену

### КРИТИЧЕСКИЕ ПРОБЛЕМЫ:
- ❌ Кнопка "Смотреть трейлер" — не работает
- ❌ Кнопка "Купить курс" — не работает
- ❌ Кнопка "Начать бесплатно" — не работает
- ❌ Нет BottomNav на странице

---

## 🛒 Список продуктов (ShoppingPage)

### Как работает:
- Базовый список из `data/shopping.js`
- Чекбоксы в `localStorage.shopping_checked`
- Можно добавлять свои продукты

### Проблемы:
- ⚠️ Видео-карточка не кликабельна
- ⚠️ Нет кнопки "Очистить всё"

---

## 🔐 Авторизация

### Как работает:
1. Регистрация через Supabase Auth (email = phone@health.app)
2. Профиль сохраняется в `profiles` через RPC `update_user_profile`
3. Данные дублируются в localStorage для быстрого доступа

### localStorage ключи:
```
user_data — { id, name, gender, age, weight, height, whatsapp }
user_name — имя для отображения
user_gender — пол
registration_date — дата регистрации
survey_completed — опросник пройден
upload_completed — файлы загружены
data_submitted — данные отправлены на анализ
results_ready — результаты готовы
onboarding_completed — онбординг пройден
notifications_enabled — уведомления включены
fasting_settings — настройки голодания
meal_plan — выбранные рецепты
food_tracker — записи дневника питания
shopping_checked — отмеченные продукты
registered_seminars — записи на семинары
health_tracker_data — витамины и daily_data
```

---

## ❌ ЧТО НЕ РАБОТАЕТ (критично)

| Проблема | Файл | Что делать |
|----------|------|------------|
| CoursePage — кнопки мёртвые | CoursePage.jsx | Добавить onClick → WhatsApp |
| CoursePage — нет BottomNav | CoursePage.jsx | Импортировать и добавить |
| VitaminsPage — удаление без confirm | VitaminsPage.jsx | Добавить модалку |
| ShoppingPage — видео не кликабельно | ShoppingPage.jsx | Добавить onClick |
| MealPlanPage — Settings не работает | MealPlanPage.jsx | Убрать или реализовать |
| MyReportPage — CTA кнопки мёртвые | MyReportPage.jsx | Добавить onClick → WhatsApp |
| MaterialsPage — эфиры не открываются | MaterialsPage.jsx | Добавить onClick |
| fasting_settings — таблица не создана | Supabase | Выполнить SQL |
| fasting_sessions — таблица не создана | Supabase | Выполнить SQL |

---

## ⚠️ ЧТО РАБОТАЕТ ЧАСТИЧНО

| Фича | Проблема |
|------|----------|
| DailyInsight | Если < 3 дней данных — всегда "Отличный день" |
| Восстановление пароля | Только UI, нет реальной отправки |
| Регистрация на семинары | Только localStorage, не в БД |
| Push-уведомления витаминов | Только UI toggle |
| StatsPage streak | Алгоритм может считать неточно |
| Превью файлов в UploadModal | Нет превью, нельзя удалить файл |
| Редактирование еды в FoodPage | Только удаление |

---

## ✅ ЧТО РАБОТАЕТ ХОРОШО

1. Регистрация и авторизация
2. Опросник (все типы вопросов)
3. Загрузка файлов анализов
4. AI-генерация отчёта
5. Отображение MyReportPage
6. HabitsTracker (вода, активность, сон)
7. Streak на главной
8. ProfilePage с ИМТ
9. WeightEditModal
10. StatsPage (базово)
11. FastingSettingsPage
12. FastingWidget (таймер)
13. BottomNav навигация
14. Lazy loading страниц
15. PWA (manifest, service worker)
16. MealPlanPage свайпы

---

## 🔧 Ключевые сервисы

### lib/supabase.ts
```js
// Инициализация клиента Supabase
// URL и ключ из .env
export const supabase = createClient(url, key)
```

### lib/generateReport.js
```js
// Вызывает Edge Function для AI-анализа
// POST → supabase.functions.invoke('generate-analysis')
```

### lib/foodAnalysisService.js
```js
// AI-анализ фото еды через Claude
// Загружает фото в Storage, вызывает Edge Function
```

### lib/recipesService.js
```js
// CRUD для рецептов из Supabase
// getAllRecipes(), saveRecipe(), etc.
```

---

## 📞 Контакты для интеграции

- **WhatsApp для заказов:** +77472370208
- **Supabase URL:** в .env (VITE_SUPABASE_URL)
- **Supabase Key:** в .env (VITE_SUPABASE_ANON_KEY)

---

## 🚀 Приоритеты разработки

### До App Store:
1. Создать таблицы fasting_settings, fasting_sessions
2. Исправить CoursePage (кнопки + BottomNav)
3. Исправить VitaminsPage (confirm удаления)
4. Добавить aria-labels везде

### После релиза:
1. Реальные push-уведомления
2. Редактирование записей еды
3. Экспорт отчёта в PDF
4. История веса с графиком
5. Синхронизация данных между устройствами

---

*Последнее обновление: 16 января 2026*
