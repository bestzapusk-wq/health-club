# QA-отчёт приложения Health Club
**Дата:** 16 января 2026  
**Версия:** После реструктуризации профиля  
**Тестировщик:** QA-инженер (женщина, 40 лет, целевая аудитория)

---

## 📊 ОБЩАЯ СТАТИСТИКА

| Категория | Количество |
|-----------|------------|
| Страниц протестировано | 18 |
| Компонентов | 39 |
| Критических багов | 7 |
| Средних проблем | 12 |
| Мелких недочётов | 15 |
| Новых фич для добавления | 8 |

---

## 🔴 КРИТИЧЕСКИЕ БАГИ (нужно исправить до релиза)

### 1. CoursePage — кнопки не работают
**Файл:** `src/pages/CoursePage.jsx`
- Кнопка "Смотреть трейлер" — ничего не делает
- Кнопка "Купить курс" — ничего не делает  
- Кнопка "Начать бесплатно" — ничего не делает
- **Нет BottomNav** на странице курса

**Решение:**
```jsx
// Добавить обработчики
const handleBuyCourse = () => {
  window.open('https://wa.me/77472370208?text=Хочу+купить+курс+' + course.title, '_blank');
};

// Добавить BottomNav
import BottomNav from '../components/layout/BottomNav';
// В конце return: <BottomNav />
```

---

### 2. ShoppingPage — видео не кликабельно
**Файл:** `src/pages/ShoppingPage.jsx`
- Видеокарточка не реагирует на клик
- Нет `aria-label` на кнопке "Назад"
- Нет кнопки "Очистить всё"

**Решение:**
```jsx
// Добавить onClick на video-card
<div className="video-card" onClick={() => window.open(SHOPPING_VIDEO.url, '_blank')}>

// aria-label
<button className="back-btn" onClick={() => navigate('/food')} aria-label="Назад">
```

---

### 3. VitaminsPage — удаление без подтверждения
**Файл:** `src/pages/VitaminsPage.jsx`
- При нажатии "Удалить" витамин удаляется моментально без confirm
- Пользователь может случайно удалить данные

**Решение:** Добавить модалку подтверждения (аналогично FoodPage)

---

### 4. MealPlanPage — Settings кнопка не работает
**Файл:** `src/pages/MealPlanPage.jsx`  
**Строка:** 248
```jsx
<button className="settings-btn">
  <Settings size={22} />
</button>
```
- Кнопка Settings ничего не делает
- Свайпы не очевидны для новых пользователей

**Решение:** Убрать кнопку или добавить функционал + добавить подсказку о свайпах

---

### 5. ProgramDetailPage — WhatsApp номер placeholder
**Файл:** `src/pages/ProgramDetailPage.jsx`
- Номер телефона для WhatsApp — placeholder

**Статус:** ✅ ИСПРАВЛЕНО (77472370208)

---

### 6. MaterialsPage — эфиры не открываются
**Файл:** `src/pages/MaterialsPage.jsx`
- Секция "Эфиры" (STREAMS) — карточки не кликабельны
- В отличие от VideoSection, Section не имеет onClick

**Решение:**
```jsx
// Добавить onClick в Section компонент
<div key={item.id} className="video-card" onClick={() => item.videoUrl && window.open(item.videoUrl, '_blank')}>
```

---

### 7. FastingSettingsPage — таблица не создана
**Файл:** Supabase
- Таблица `fasting_settings` не существует в БД
- При сохранении будет ошибка

**Решение:** Выполнить SQL из документации:
```sql
CREATE TABLE IF NOT EXISTS fasting_settings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES profiles(id) UNIQUE,
  mode TEXT NOT NULL DEFAULT '16:8',
  eating_window_start TIME NOT NULL DEFAULT '12:00',
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

ALTER TABLE fasting_settings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage own fasting" ON fasting_settings
  FOR ALL USING (auth.uid() = user_id);
```

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### 8. StatsPage — неточный расчёт streak
**Файл:** `src/pages/StatsPage.jsx`
- Streak считается неправильно если пользователь пропустил день
- `bestStreak` может быть некорректным

---

### 9. ProfilePage — ИМТ показывает цвет в белой шапке
**Файл:** `src/pages/ProfilePage.jsx`
- Цвет `bmiStatus.color` плохо виден на синем фоне (особенно зелёный)

**Решение:** Использовать светлые оттенки или белый с эмодзи

---

### 10. DailyInsight — нет данных за 7 дней
**Файл:** `src/components/habits/DailyInsight.jsx`
- Если у пользователя меньше 3 дней данных, всегда показывает "Отличный день!"
- Логика не учитывает что данных может быть мало

---

### 11. RegisterPage — восстановление пароля
**Файл:** `src/pages/RegisterPage.jsx`
- Кнопка "Восстановить доступ" только показывает "Запрос отправлен"
- Реальной интеграции с WhatsApp нет

---

### 12. MyReportPage — CTA кнопки не работают
**Файл:** `src/pages/MyReportPage.jsx`
- "Записаться на консультацию" — нет onClick
- "Написать в WhatsApp" — нет onClick

**Решение:**
```jsx
<button className="cta-button-new" onClick={() => window.open('https://wa.me/77472370208', '_blank')}>
```

---

### 13. SurveyPage — нет сохранения при выходе
**Файл:** `src/pages/SurveyPage.jsx`
- При случайном закрытии браузера — прогресс теряется
- Нет автосохранения

---

### 14. Header — аватар не кликабельный
**Файл:** `src/components/layout/Header.jsx`
- Аватар в шапке не ведёт на профиль
- Пользователь интуитивно ожидает этого

---

### 15. UploadModal — нет превью файлов
**Файл:** `src/components/main/UploadModal.jsx`
- После загрузки не видно какие файлы загружены
- Нельзя удалить отдельный файл

---

### 16. FoodPage — нельзя редактировать запись
**Файл:** `src/pages/FoodPage.jsx`
- Есть кнопка удаления, но нет редактирования
- Import `Edit3` не используется

---

### 17. RecipesPage — сохранённые рецепты не кликабельны
**Файл:** `src/pages/RecipesPage.jsx` (предполагаю)
- Сохранённые рецепты показываются, но не открываются на просмотр

---

### 18. SavedPage — пустая страница?
**Файл:** `src/pages/SavedPage.jsx`
- Нужно проверить что показывает эта страница

---

### 19. DiaryPage — дублирование FoodPage?
**Файл:** `src/pages/DiaryPage.jsx`
- Есть две страницы дневника? Путаница в навигации

---

## 🟢 МЕЛКИЕ НЕДОЧЁТЫ

### 20. Accessibility — отсутствуют aria-labels
**Файлы:**
- `ShoppingPage.jsx` — back-btn
- `MealPlanPage.jsx` — settings-btn  
- `CoursePage.jsx` — back-btn
- `MaterialsPage.jsx` — slider buttons

---

### 21. WeightEditModal — нет индикатора загрузки
- При сохранении нет спиннера, только текст "Сохраняем..."

---

### 22. ProfilePage — телефон не показывается
**Строка удалена:** `profile-phone` класс есть в CSS, но не используется

---

### 23. HabitsTracker — кнопки без aria-labels
- Кнопки +/- воды без aria-label

---

### 24. Chips активность — 0 мин не выбрать
**Файл:** `src/components/habits/HabitsTracker.jsx`
- ACTIVITY_OPTIONS начинаются с 15 мин
- Нельзя записать 0 минут активности

---

### 25. MainPage — неиспользуемый import Sparkles
**Файл:** `src/pages/MainPage.jsx`
- `Sparkles` импортируется но не используется

---

### 26. Survey — долго без индикатора
- При переходе между вопросами нет загрузки, но иногда подвисает

---

### 27. Onboarding — нельзя пропустить
- Нет кнопки "Пропустить" для опытных пользователей

---

### 28. CSS — дублирование стилей
- В ProfilePage.css есть старые стили (.profile-header) и новые (.profile-header-new)
- Нужна очистка

---

### 29. Materials — слайдер без индикаторов
- Непонятно сколько программ в слайдере

---

### 30. StatsPage — нет кнопки обновления
- Нельзя обновить статистику без перезагрузки

---

### 31. FastingSettingsPage — время только с 08:00
- TIME_OPTIONS начинаются с 08:00, но некоторые люди едят раньше

---

### 32. Семинары — регистрация в localStorage
- Регистрация на семинар сохраняется только локально
- При смене устройства — теряется

---

### 33. Нет темной темы
- Приложение не поддерживает prefers-color-scheme: dark

---

### 34. Нет skeleton для программ
- При загрузке MaterialsPage нет скелетонов для программ

---

## 🆕 РЕКОМЕНДАЦИИ ПО НОВОМУ ФУНКЦИОНАЛУ

### 1. Push-уведомления для витаминов
- Реальные push вместо просто UI переключателя

### 2. История веса с графиком
- В StatsPage добавить график изменения веса

### 3. Экспорт отчёта в PDF
- Кнопка "Скачать" в MyReportPage

### 4. Offline режим
- Service Worker уже есть, добавить кеширование данных

### 5. Синхронизация между устройствами
- Сейчас много данных в localStorage

### 6. Поиск по рецептам
- В MealPlanPage добавить фильтр/поиск

### 7. Уведомление о воде
- Напоминание пить воду каждые 2 часа

### 8. Интеграция с Apple Health / Google Fit
- Автоматический импорт шагов и сна

---

## ✅ ЧТО РАБОТАЕТ ХОРОШО

1. **Регистрация** — валидация работает отлично
2. **Авторизация** — вход по номеру работает
3. **HabitsTracker** — удобный интерфейс
4. **DailyInsight** — новая фича работает корректно
5. **FoodPage** — удаление с подтверждением ✓
6. **ProfilePage** — новая структура чистая
7. **WeightEditModal** — валидация и сохранение ✓
8. **StatsPage** — красивый дизайн
9. **FastingSettingsPage** — понятный UI
10. **Privacy Policy** — страница есть
11. **Lazy loading** — страницы загружаются быстро
12. **PWA manifest** — приложение можно добавить на экран
13. **Responsive** — адаптация под мобильные

---

## 📋 ПРИОРИТЕТНЫЙ ПЛАН ИСПРАВЛЕНИЙ

### До App Store (КРИТИЧНО):
1. ✅ WhatsApp номер в ProgramDetailPage
2. ⬜ Кнопки в CoursePage (трейлер, купить)
3. ⬜ BottomNav в CoursePage
4. ⬜ Подтверждение удаления в VitaminsPage
5. ⬜ SQL для fasting_settings в Supabase
6. ⬜ aria-labels везде
7. ⬜ CTA кнопки в MyReportPage

### После релиза (СРЕДНИЕ):
1. ⬜ Видео в ShoppingPage кликабельно
2. ⬜ Settings кнопка в MealPlanPage
3. ⬜ Эфиры кликабельны в MaterialsPage
4. ⬜ Редактирование записей в FoodPage
5. ⬜ Превью файлов в UploadModal

### Улучшения UX:
1. ⬜ Автосохранение опросника
2. ⬜ Skeleton loaders везде
3. ⬜ Подсказка о свайпах
4. ⬜ Аватар кликабельный → профиль

---

## 🔧 БЫСТРЫЕ ФИКСЫ (копипаст)

### CoursePage — добавить BottomNav и обработчики:
```jsx
import BottomNav from '../components/layout/BottomNav';
import { useState } from 'react';
import { MessageCircle, X } from 'lucide-react';

// В компоненте:
const [showBuyModal, setShowBuyModal] = useState(false);

const handleBuyCourse = () => {
  window.open(`https://wa.me/77472370208?text=Хочу+купить+курс+${encodeURIComponent(course.title)}`, '_blank');
};

// Кнопка "Купить курс":
<button className="cta-btn paid" onClick={() => setShowBuyModal(true)}>

// В конце перед закрывающим </div>:
<BottomNav />
```

### VitaminsPage — добавить подтверждение:
```jsx
const [deleteTarget, setDeleteTarget] = useState(null);

const confirmDelete = (id) => {
  setDeleteTarget(id);
};

const handleDelete = () => {
  saveVitamins(vitamins.filter(v => v.id !== deleteTarget));
  setDeleteTarget(null);
};

// Вместо onClick={() => handleDelete(vitamin.id)}:
onClick={() => confirmDelete(vitamin.id)}

// Модалка в конце:
{deleteTarget && (
  <div className="modal-overlay" onClick={() => setDeleteTarget(null)}>
    <div className="vitamin-modal" onClick={e => e.stopPropagation()}>
      <h3>Удалить витамин?</h3>
      <p>Это действие нельзя отменить</p>
      <div style={{display:'flex',gap:'12px',marginTop:'16px'}}>
        <button className="cancel-btn" onClick={() => setDeleteTarget(null)}>Отмена</button>
        <button className="save-btn" onClick={handleDelete}>Удалить</button>
      </div>
    </div>
  </div>
)}
```

### MyReportPage — CTA кнопки:
```jsx
<button className="cta-button-new" onClick={() => window.open('https://wa.me/77472370208?text=Хочу+записаться+на+консультацию', '_blank')}>
  <Phone size={20} />
  Записаться на консультацию
</button>

<button className="cta-whatsapp-new" onClick={() => window.open('https://wa.me/77472370208', '_blank')}>
  <MessageCircle size={18} />
  Написать в WhatsApp
</button>
```

---

*Отчёт готов для передачи разработчику*
